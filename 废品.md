# 积木库（BrickLibrary）

与 **StorySummary（故事库）** 结构相同，但只展示「作品列表」里的积木卡片；点「选择」后**直接进入教程场景**（不经过绘本 StoryPrologue）。顶栏标题使用 `Assets/Art/积木库.png`（Canvas 上的 HeaderTitle 图片）。

## 一次性搭建（场景被删后恢复）

1. 等脚本编译完成。
2. Unity 菜单：**StoryBricks → 搭建 BrickLibrary 积木作品集**
3. 会生成 `Assets/Scenes/BrickLibrary.unity`、加入 Build Settings，并预置「兔子」卡片。

## 新增一个积木作品 + 教程

### 1. 导出步骤图（BrickLink Studio）

在 Studio 中导出每步 PNG，或从 [Rebrickable](https://rebrickable.com/) 找模型生成教程图。

### 2. 放入工程

在 `Assets/Step/` 下建子文件夹，例如 `Assets/Step/MyModel/`，把 PNG 拖进去。

### 3. 纹理设为 Sprite

选中 PNG → Inspector → **Texture Type: Sprite (2D and UI)** → Apply。

### 4. 创建教程配置

Project 窗口右键 → **Create → StoryBricks → Tutorial Steps Config**。

### 5. 填写 Inspector（参考兔子）

| 字段 | 示例 |
|------|------|
| Title | 我的模型拼装教程 |
| Portfolio Scene Name | `BrickLibrary` |
| Steps | 按顺序拖入 `Assets/Step/MyModel/` 下的 Sprite |

也可用菜单 **StoryBricks → 教程 → 重建 Rabbit 步骤配置** 的方式，自行写类似 Editor 脚本扫描文件夹。

### 6. 新建教程场景

复制 `RabbitTutorial.unity`，改名如 `MyModelTutorial.unity`；场景里 `TutorialStepsPageBootstrap` 的 **Config** 指向你的 `TutorialStepsConfig`。

### 7. 加入 Build Settings

**File → Build Settings → Add Open Scenes**（或搭建菜单会自动加 BrickLibrary；新教程场景需手动加入）。

### 8. 在积木作品集加卡片

打开 **BrickLibrary** 场景 → 选中 **BrickPortfolio** → **作品列表 Works** → **Size +1**：

| 字段 | 说明 |
|------|------|
| Title | 卡片标题，如「兔子」 |
| Tutorial Scene Name | 教程场景名，须在 Build Settings，如 `RabbitTutorial` |
| Thumbnail | 卡片封面 Sprite（可用手搓图或步骤图第一张） |

保存场景。

## 与故事库的区别

| | StorySummary | BrickLibrary |
|--|--------------|--------------|
| 场景名 | StorySummary | BrickLibrary |
| 数据来源 | StoryCatalog / Resources/Stories | 仅 **作品列表 Works** |
| 点选后 | StoryPrologue → 搭建教程 | **直接**进教程场景 |
| 教程返回 | StorySummary | BrickLibrary（Config 里 Portfolio Scene Name） |

## 入口场景

封面 **StartScene** 默认进入故事库。若要进积木作品集，可复制「开始」按钮，把 `StartSceneLoadButton` 的 **Next Scene Name** 设为 `BrickLibrary`，或从其它场景 `LoadScene("BrickLibrary")`。
